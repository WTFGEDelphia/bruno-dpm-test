info:
  name: 删除任务依赖
  type: http
  seq: 6
  tags:
    - 任务依赖管理

http:
  method: POST
  url: "{{baseUrl}}/api/v1/scenario-tasks/:taskId/dependencies/delete"
  params:
    - name: taskId
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "id": 0
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/scenario-tasks/:taskId/dependencies/delete"
      method: POST
      params:
        - name: taskId
          value: ""
          type: path
      body:
        type: json
        data: |-
          {
            "id": 0
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "code": 0,
            "reason": "",
            "data": false,
            "timestamp": 0
          }
