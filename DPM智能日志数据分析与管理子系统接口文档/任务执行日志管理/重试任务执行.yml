info:
  name: 重试任务执行
  type: http
  seq: 3
  tags:
    - 任务执行日志管理

http:
  method: POST
  url: "{{baseUrl}}/task-executions/retry"
  body:
    type: json
    data: |-
      {
        "jobId": 0
      }
  headers:
    - name: Cookie
      value: tokenId={{applicantToken}}
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/task-executions/retry"
      method: POST
      body:
        type: json
        data: |-
          {
            "jobId": 0
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "code": 0,
            "reason": "",
            "data": 0,
            "timestamp": 0
          }
