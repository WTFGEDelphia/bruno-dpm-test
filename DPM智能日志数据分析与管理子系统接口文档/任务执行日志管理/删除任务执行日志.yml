info:
  name: 删除任务执行日志
  type: http
  seq: 11
  tags:
    - 任务执行日志管理

http:
  method: POST
  url: "{{baseUrl}}/api/v1/task-executions/delete"
  body:
    type: json
    data: |-
      {
        "jobId": 0
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/task-executions/delete"
      method: POST
      body:
        type: json
        data: |-
          {
            "jobId": 0
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "code": 0,
            "reason": "",
            "data": false,
            "timestamp": 0
          }
