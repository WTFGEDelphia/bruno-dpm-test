info:
  name: 删除任务执行日志 (POST)
  type: http
  seq: 12
  tags:
    - 任务执行日志管理

http:
  method: POST
  url: "{{baseUrl}}/task-executions/delete"
  body:
    type: json
    data: |-
      {
        "jobId": 0
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/task-executions/delete"
      method: POST
      body:
        type: json
        data: |-
          {
            "jobId": 0
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "code": 0,
            "reason": "",
            "data": false,
            "timestamp": 0
          }
