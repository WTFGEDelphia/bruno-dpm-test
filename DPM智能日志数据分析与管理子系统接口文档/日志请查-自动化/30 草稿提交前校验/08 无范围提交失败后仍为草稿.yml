info:
  name: 08 无范围提交失败后仍为草稿
  type: http
  seq: 8
  tags:
    - 日志请查
    - 自动化
    - P1
    - 草稿提交

http:
  method: POST
  url: "{{baseUrl}}/api/v1/log-requests/detail"
  body:
    type: json
    data: |-
      {
        "id": {{emptyScopeDraftId}}
      }
  auth:
    type: bearer
    token: "{{applicantToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("提交失败后无范围申请仍为草稿", () => {
          expect(res.getStatus()).to.equal(200);
          const json = res.getBody();
          expect(json).to.be.an("object");
          expect(json.code).to.equal(200);
          expect(["success", "成功"]).to.include(json.reason);
          expect(json.data.id).to.equal(Number(bru.getVar("emptyScopeDraftId")));
          expect(json.data.status).to.equal("DRAFT");
          expect(json.data.requestReason).to.equal("草稿提交前校验-缺少调取范围");
          expect(json.data.scopes || []).to.have.length(0);
          expect(json.data.flowLogs || []).to.have.length(0);
          const text = JSON.stringify(json.data);
          expect(text).to.not.contain("storagePath");
          expect(text).to.not.contain("/Users/");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
