info:
  name: 38 时间字段格式错误
  type: folder
  seq: 38

request:
  auth: inherit

docs: |-
  P1 时间字段格式错误链路。覆盖日志调取范围和列表创建时间筛选字段传入非 yyyy-MM-dd HH:mm:ss 格式时的统一错误。
  失败响应不能退化为 HTML/Whitelabel，不能泄露 Jackson/Spring 异常类名或本地路径。
