info:
  name: 02 查询紧急审批前详情
  type: http
  seq: 2
  tags:
    - 日志请查
    - 自动化
    - P0
    - 紧急审批

http:
  method: POST
  url: "{{baseUrl}}/api/v1/log-requests/detail"
  body:
    type: json
    data: |-
      {
        "id": {{urgentRequestId}}
      }
  headers:
    - name: Cookie
      value: tokenId={{applicantToken}}
runtime:
  scripts:
    - type: tests
      code: |-
        test("紧急申请审批前为 PENDING", () => {
          expect(res.getStatus()).to.equal(200);
          const json = res.getBody();
          expect(json.code).to.equal(200);
          expect(["success", "成功"]).to.include(json.reason);
          expect(json.data.id).to.equal(Number(bru.getVar("urgentRequestId")));
          expect(json.data.status).to.equal("PENDING");
          expect(json.data.urgent).to.equal(true);
          expect(json.data.authorization || null).to.equal(null);
          expect(json.data.requestNo).to.be.a("string").and.not.empty;
          bru.setVar("urgentRequestNo", json.data.requestNo);
        });
  actions:
    - type: set-variable
      phase: after-response
      selector:
        expression: data.requestNo
        method: jsonq
      variable:
        name: urgentRequestNo
        scope: runtime

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
