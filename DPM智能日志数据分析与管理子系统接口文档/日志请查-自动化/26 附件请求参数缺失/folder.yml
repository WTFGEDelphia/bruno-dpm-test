info:
  name: 26 附件请求参数缺失
  type: folder
  seq: 26

request:
  auth: inherit

docs: |-
  P1 附件 @RequestParam 缺失校验。第 18 组已覆盖缺少 multipart file，本组补齐上传缺少 requestId、上传缺少 attachmentType、下载缺少 attachmentId。
  这些场景应由统一异常处理返回 Result 错误，不应返回框架默认错误页或文件流。
