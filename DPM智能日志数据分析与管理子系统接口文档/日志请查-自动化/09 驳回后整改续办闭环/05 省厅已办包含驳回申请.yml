info:
  name: 05 省厅已办包含驳回申请
  type: http
  seq: 5
  tags:
    - 日志请查
    - 自动化
    - P0
    - 驳回整改

http:
  method: POST
  url: "{{baseUrl}}/api/v1/log-requests/province/done/page"
  body:
    type: json
    data: |-
      {
        "pageNum": 1,
        "pageSize": 10,
        "requestNo": "{{rejectResubmitRequestNo}}",
        "status": "REJECTED",
        "urgent": false,
        "targetOrgId": "{{targetOrgId}}"
      }
  headers:
    - name: Cookie
      value: tokenId={{applicantToken}}
runtime:
  scripts:
    - type: tests
      code: |-
        test("省厅已办包含驳回申请", () => {
          expect(res.getStatus()).to.equal(200);
          const json = res.getBody();
          expect(json.code).to.equal(200);
          expect(["success", "成功"]).to.include(json.reason);
          const requestId = Number(bru.getVar("rejectResubmitRequestId"));
          const records = json.data.records || [];
          const matched = records.find(item => item.id === requestId);
          expect(matched, "省厅已办应包含驳回申请").to.exist;
          expect(matched.status).to.equal("REJECTED");
          expect(matched.directiveStatus).to.equal("NONE");
          expect(matched.requestNo).to.equal(bru.getVar("rejectResubmitRequestNo"));
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
