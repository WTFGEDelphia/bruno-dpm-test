info:
  name: 05 查询申请A附件
  type: http
  seq: 5
  tags:
    - 日志请查
    - 自动化
    - P2
    - 附件边界

http:
  method: POST
  url: "{{baseUrl}}/api/v1/log-requests/detail"
  body:
    type: json
    data: |-
      {
        "id": {{fileBoundaryRequestAId}}
      }
  headers:
    - name: Cookie
      value: tokenId={{applicantToken}}
runtime:
  scripts:
    - type: tests
      code: |-
        function activeApplicationAttachments(attachments) {
          return (attachments || []).filter(item => item.attachmentType === "APPLICATION" && item.deleted !== true);
        }

        test("申请 A 包含 doc/xlsx/pdf 三个允许扩展名附件", () => {
          expect(res.getStatus()).to.equal(200);
          const json = res.getBody();
          expect(json.code).to.equal(200);
          expect(["success", "成功"]).to.include(json.reason);
          const attachments = activeApplicationAttachments(json.data.attachments);
          expect(attachments).to.have.length(3);
          expect(attachments.map(item => item.fileName)).to.include("allowed-attachment.doc");
          expect(attachments.map(item => item.fileName)).to.include("allowed-attachment.xlsx");
          expect(attachments.map(item => item.fileName)).to.include("allowed-attachment.pdf");
          expect(JSON.stringify(json.data.attachments || [])).to.not.contain("storagePath");
          expect(JSON.stringify(json.data.attachments || [])).to.not.contain("/Users/");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
