info:
  name: 22 必填字段校验
  type: folder
  seq: 22

request:
  auth: inherit

docs: |-
  P1 创建申请必填字段校验。覆盖 LogRequestCreateVO 与 LogRequestScopeVO 的必填字段缺失场景，确保正式申请创建不会绕过参数校验，且失败响应不泄露存储路径或本地路径。
  覆盖 LR-P1-101：targetOrgId、targetOrgName、validDays、urgent、requestReason、scopes 及范围明细字段缺失必须被拒绝。
