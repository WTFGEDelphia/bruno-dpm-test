info:
  name: 查询节假日
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/v1/query-support/vacations/list"
  body:
    type: json
    data: "{}"
  headers:
    - name: Cookie
      value: tokenId={{applicantToken}}


settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
