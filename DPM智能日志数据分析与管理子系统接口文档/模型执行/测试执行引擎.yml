info:
  name: 测试执行引擎
  type: http
  seq: 1
  tags:
    - 模型执行

http:
  method: POST
  url: "{{baseUrl}}/api/model/execution/test"
  headers:
    - name: Cookie
      value: tokenId={{applicantToken}}
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/model/execution/test"
      method: POST
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: ""

docs: 测试模型执行引擎的基本功能
