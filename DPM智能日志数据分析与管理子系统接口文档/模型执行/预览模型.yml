info:
  name: 预览模型
  type: http
  seq: 2
  tags:
    - 模型执行

http:
  method: POST
  url: "{{baseUrl}}/api/model/execution/preview"
  body:
    type: json
    data: |-
      {
        "modelId": 0
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/model/execution/preview"
      method: POST
      body:
        type: json
        data: |-
          {
            "modelId": 0
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: "{}"

docs: 预览指定ID的分析模型（生成逻辑表达式和SQL，但不执行）
