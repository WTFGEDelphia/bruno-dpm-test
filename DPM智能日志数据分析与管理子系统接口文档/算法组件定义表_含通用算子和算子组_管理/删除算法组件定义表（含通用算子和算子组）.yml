info:
  name: 删除算法组件定义表（含通用算子和算子组）
  type: http
  seq: 4
  tags:
    - 算法组件定义表_含通用算子和算子组_管理

http:
  method: POST
  url: "{{baseUrl}}/algorithmComponent/delete"
  body:
    type: json
    data: |-
      {
        "id": 0
      }
  headers:
    - name: Cookie
      value: tokenId={{applicantToken}}
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/algorithmComponent/delete"
      method: POST
      body:
        type: json
        data: |-
          {
            "id": 0
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: "false"
