info:
  name: 43 附件删除字段类型错误
  type: folder
  seq: 43

request:
  auth: inherit

docs: |-
  P1 附件删除字段类型错误链路。覆盖 requestId、attachmentId 和 attachmentType 类型错误时的统一错误。
  删除失败时不能误返回 data=true，且不能泄露框架异常、本地路径或 storagePath。
