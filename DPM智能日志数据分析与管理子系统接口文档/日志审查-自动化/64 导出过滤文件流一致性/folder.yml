info:
  name: 64 导出过滤文件流一致性
  type: folder
  seq: 64

request:
  auth: inherit

docs: |-
  P1 导出过滤文件流一致性链路。创建一条可导出的城市侧待审核申请后，分别使用命中筛选与不命中筛选导出，校验两次均返回 Excel 文件流，且命中导出的文件体积大于空结果导出，侧面证明 requestNo/status/urgent/targetOrgId 过滤条件参与导出数据集。
