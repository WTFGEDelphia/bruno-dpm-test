info:
  name: 27 附件删除参数校验
  type: folder
  seq: 27

request:
  auth: inherit

docs: |-
  P1 附件删除 JSON 请求体参数校验。覆盖 requestId、attachmentId、attachmentType 缺失，以及空 JSON 请求体，确保删除接口走统一错误响应且不会误删除附件。
  对应 LogRequestAttachmentDeleteVO 的 @NotNull 约束和服务层 “附件删除参数不能为空” 兜底。
