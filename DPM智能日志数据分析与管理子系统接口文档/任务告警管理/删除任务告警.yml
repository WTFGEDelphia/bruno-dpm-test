info:
  name: 删除任务告警
  type: http
  seq: 6
  tags:
    - 任务告警管理

http:
  method: POST
  url: "{{baseUrl}}/api/v1/task-alarms/delete"
  body:
    type: json
    data: |-
      {
        "jobId": 0
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/task-alarms/delete"
      method: POST
      body:
        type: json
        data: |-
          {
            "jobId": 0
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "code": 0,
            "reason": "",
            "data": false,
            "timestamp": 0
          }
