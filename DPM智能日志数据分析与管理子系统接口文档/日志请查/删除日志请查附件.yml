info:
  name: 删除日志请查附件
  type: http
  seq: 17
  tags:
    - 日志请查

http:
  method: POST
  url: "{{baseUrl}}/api/v1/log-requests/attachments/delete"
  body:
    type: json
    data: |-
      {
        "requestId": 0,
        "attachmentId": 0,
        "attachmentType": "APPLICATION"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/log-requests/attachments/delete"
      method: POST
      body:
        type: json
        data: |-
          {
            "requestId": 0,
            "attachmentId": 0,
            "attachmentType": "APPLICATION"
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "code": 0,
            "reason": "",
            "data": false,
            "timestamp": 0
          }
