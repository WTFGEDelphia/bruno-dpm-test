info:
  name: 删除算法模板表
  type: http
  seq: 5
  tags:
    - 算法模板表管理

http:
  method: POST
  url: "{{baseUrl}}/algorithmTemplate/delete"
  body:
    type: json
    data: |-
      {
        "id": 0
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/algorithmTemplate/delete"
      method: POST
      body:
        type: json
        data: |-
          {
            "id": 0
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: "false"
