info:
  name: 从模型创建版本
  type: http
  seq: 7
  tags:
    - 模型版本表管理

http:
  method: POST
  url: "{{baseUrl}}/modelVersion/createFromModel"
  body:
    type: json
    data: |-
      {
        "modelId": 0,
        "changeLog": ""
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/modelVersion/createFromModel"
      method: POST
      body:
        type: json
        data: |-
          {
            "modelId": 0,
            "changeLog": ""
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: "0"
