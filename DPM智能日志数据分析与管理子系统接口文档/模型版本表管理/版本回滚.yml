info:
  name: 版本回滚
  type: http
  seq: 2
  tags:
    - 模型版本表管理

http:
  method: POST
  url: "{{baseUrl}}/modelVersion/rollback"
  body:
    type: json
    data: |-
      {
        "modelId": 0,
        "versionId": 0
      }
  headers:
    - name: Cookie
      value: tokenId={{applicantToken}}
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/modelVersion/rollback"
      method: POST
      body:
        type: json
        data: |-
          {
            "modelId": 0,
            "versionId": 0
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: "false"
