info:
  name: 重置所有熔断器
  type: http
  seq: 3
  tags:
    - 熔断器管理

http:
  method: POST
  url: "{{baseUrl}}/api/circuit-breaker/reset-all"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/circuit-breaker/reset-all"
      method: POST
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: "{}"

docs: 将所有熔断器重置到初始状态
