info:
  name: 查询所有熔断器状态
  type: http
  seq: 4
  tags:
    - 熔断器管理

http:
  method: POST
  url: "{{baseUrl}}/api/circuit-breaker/list"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/circuit-breaker/list"
      method: POST
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: "{}"

docs: 获取所有已注册熔断器的当前状态
